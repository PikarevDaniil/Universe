Иногда при передаче часть данных искажается. Чтобы бороться с этим придумали различные методы:

### Бит чётности
К двоичному сообщению добаляется один бит так, чтобы количество единиц в сообщении оказалось чётным.
Если искажён один бит, сумма битов станет нечётной и ошибка найдётся. Но если искажений чётное количество, то сумма останется чётной и ошибка и ошибка не найдётся
### Троирование
Берём каждый бит исходного сообщения, а в новое записываем его три раза.
Таким образом, новое сообщение состоит из блоков по три одинаковх бита. Если в каждом блоке будет не больше одного искажения, то его удастся корректно прочитать. Происходит это потому, что при чтении блока, оно расшифровывается в тот бит, который повторяется в нём хотя бы 2 раза.
### Код Хэмминга
Код Хэмминга состоит из 7 бит: информационных 3, 5, 6, 7 и контрольных 1, 2, 4.

Составление кода Хэмминга из четырёхбитного сообщения
```python
d1, d2, d3, d4 = msg
p1 = d1 ^ d2 ^ d4
p2 = d1 ^ d3 ^ d4
p4 = d2 ^ d3 ^ d4
res = [p1, p2, d1, p4, d2, d3, d4]
```

Вычисление синдрома
```python
s1 = code[0] ^ code[2] ^ code[4] ^ code[6]
s2 = code[1] ^ code[2] ^ code[5] ^ code[6]
s4 = code[3] ^ code[4] ^ code[5] ^ code[6]
syn = s1 * 1 + s2 * 2 + s4 * 4
```

Если `syn == 0`, то ошибок нет. В ином случае, ошибка находится в бите с индексом `syn - 1`. Чтобы исправить ошибку, зная её индекс, достаточно сделать `code[syn-1] ^= 1`